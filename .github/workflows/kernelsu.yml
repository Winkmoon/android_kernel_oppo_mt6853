name: 构建PERM00_Android12. the `actions/upload-artifact@v3` action. Ensure that1内核-支持KernelSU

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses the action is correctly specified and that it is available in the GitHub Marketplace.

1. Verify the `kernelsu.yml` workflow file to check the usage of: actions/checkout@v3

    - name: 构建编译内核环境
      run: |
        sudo apt-get update
        sudo - `actions/upload-artifact@v4`.

2. Update the workflow fileE apt-get -y -qq install git make bc bison ccache opens if there are any syntax errors or version mismatches.

Here issl dos2unix zip kmod c an example of how the `upload-artifactpio flex libelf-dev curl libssl-dev libtfm-dev wget device-tree-compiler ca-cert` action should be used in your workflow file:

```ificates python3 python2.7

    - name: 下载 Gcc编yaml
name: Build Kernel

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
译器
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        mkdir gcc64
        wget -O gcc-aarch64.tar.gz https://android.googlesource.com/platform/preb      uses: actions/checkout@v2

    - name: Set up ccache
      uses: hendrikmuhs/ccache-action@v1.2

    - name: Build Kernel
      run: |
        make defconfig
        make -j$(nproc)

    - name: Upload Kernel
      uses: actions/upload-artifact@v3
      withuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9:
        name: kernel
        path: path/to/kernel/output/+archive/refs/tags/android-12.1.0_r27.tar.gz
       
